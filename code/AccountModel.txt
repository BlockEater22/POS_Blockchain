class AccountModel():

    def __init__(self):
        self.accounts = [] #this is a empty list storing the value of a public key 
        self.balances = {} #this is an empty dictionary storing the balances of account

    def addAccount(self, publicKeyString):
    """this Method addAccount(public key) if it is not present in the list and set the balance to 0"""
        

    def getBalance(self, publicKeyString):
        """this Method add Account(public key) to the list if it is not present in the list and get the balance of that accounts"""
        

    def updateBalance(self, publicKeyString, amount):
    """This Method check if the public key is not present in list  it adds the public key  to list and then update their balance"
        
